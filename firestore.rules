rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For development:
    // - allow reads for everyone (so public pages and draw listing work)
    // - allow writes only for authenticated users
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
